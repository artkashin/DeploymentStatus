# ????????? ????????? ??????

?????? ???????????? ??? ???? ?????????:
1. **In-Memory** (?? ?????????) - ?????? ???????? ? ??????, ???????? ??? ???????????
2. **Azure Table Storage** - ?????????? ????????? ? Azure

## ????????????? In-Memory (??????????)

?? ????????? ???????????? in-memory ?????????. ? `local.settings.json`:

```json
{
  "Values": {
    "StorageType": "InMemory"
  }
}
```

## ????????????? Azure Table Storage

### ????????? ?????????? ? Azurite

1. ?????????? Azurite (???????? Azure Storage):
```powershell
npm install -g azurite
```

2. ????????? Azurite:
```powershell
azurite --silent --location c:\azurite --debug c:\azurite\debug.log
```

3. ???????? `local.settings.json`:
```json
{
  "Values": {
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "StorageType": "TableStorage"
  }
}
```

4. ????????? Functions:
```powershell
.\rebuild-and-start.ps1
```

### Production ? ???????? Azure Storage

1. ???????? Storage Account ? Azure Portal:
   - ???: StorageV2
   - Performance: Standard
   - Replication: LRS (??? ?????? ?? ?????????????)

2. ?????????? Connection String:
   - ? Azure Portal ? Storage Account ? Access keys
   - ?????????? Connection string

3. ???????? ????????? ??????????:

**??? ????????? ??????????** (`local.settings.json`):
```json
{
  "Values": {
    "AzureWebJobsStorage": "DefaultEndpointsProtocol=https;AccountName=youraccount;AccountKey=yourkey;EndpointSuffix=core.windows.net",
    "StorageType": "TableStorage"
  }
}
```

**??? Azure Functions App**:
```powershell
# ????? Azure CLI
az functionapp config appsettings set --name <function-app-name> --resource-group <resource-group> --settings "StorageType=TableStorage"
```

??? ????? Azure Portal:
- Function App ? Configuration ? Application settings
- ???????? `StorageType` = `TableStorage`

## ????????? ?????? ? Azure Table Storage

### ???????: Deployments
- **PartitionKey**: ClientId
- **RowKey**: ApplicationId_Timestamp
- ????????: ClientName, ApplicationId, ApplicationName, Version, DeploymentTime, Status

### ???????: CiCdVersion
- **PartitionKey**: "CiCdVersion"
- **RowKey**: "Current"
- ????????: Version, UpdatedAt, UpdatedBy, Notes

## ???????????? ????? ?????? ?????????

?????? ???????? ???????? `StorageType` ? ??????????:
- `InMemory` - in-memory ?????????
- `TableStorage` - Azure Table Storage

**?????**: ??? ???????????? ????? ?????? ????????? ?????? ?? ??????????? ?????????????.

## ??????? ??? ?????? ? Azurite

### ?????? Azurite
```powershell
.\start-azurite.ps1
```

### ??????? ?????? Azurite
```powershell
azurite-clean
Remove-Item c:\azurite\* -Recurse -Force
```

## ?????????? ? ???????????

### ???????? ?????? ? Table Storage

**Azure Storage Explorer**:
1. ????????: https://azure.microsoft.com/features/storage-explorer/
2. ???????????? ? Azurite (Local Emulator) ??? ?????? Azure Storage Account
3. ???????? Tables ? Deployments / CiCdVersion

**PowerShell**:
```powershell
# ?????????? ??????
Install-Module -Name Az.Storage

# ????????????
$context = New-AzStorageContext -ConnectionString "UseDevelopmentStorage=true"

# ???????? ??????
Get-AzStorageTable -Name "Deployments" -Context $context | Get-AzTableRow
```

## ????????? Azure Table Storage

????????? ????????? ??? 15 ???????? × 30 ?????????? × ?????????? ??????????:
- ????????: ~0.5 GB/????? ? $0.02/?????
- ??????????: ~1000 ????????/???? ? $0.01/?????
- **?????: ~$0.03-0.05/?????**

Azure Table Storage - ???? ?? ????? ??????? ????????? ???????? ? Azure!
